<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Цвет ripple -->

    <!-- Маска для формы ripple -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" /> <!-- Такие же скругления! -->
            <solid android:color="@color/black" /> <!-- Любой цвет, главное - форма -->
        </shape>
    </item>

    <!-- Фон кнопки -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp"/>
            <solid android:color="@color/black" />
            <stroke
                android:width="4dp"
                android:color="@color/white"
                android:padding="5dp"/><!-- Основной цвет кнопки -->
        </shape>
    </item>

</ripple>